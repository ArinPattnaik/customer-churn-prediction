# Backend API URL — set this to your deployed backend
# Leave empty for same-origin (when frontend is served by Flask)
# Set to your Render URL when deploying frontend separately (e.g., on Vercel)
NEXT_PUBLIC_API_URL=https://churnguard.onrender.com
